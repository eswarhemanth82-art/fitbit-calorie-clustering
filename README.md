# fitbit-calorie-clustering
ML project predicting calorie burn from Fitbit sensor data using regression models (XGBoost, Random Forest, etc.) and discovering hidden workout patterns via PCA + KMeans clustering. Built with Streamlit for interactive demo.
